const home = require('./home')
const course = require('./course')
const auth = require('./auth')
const profile = require('./profile')
const admin = require('./admin')

module.exports = { home, course, auth, profile, admin }